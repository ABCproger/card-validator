// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: card/v1/card.proto

package cardv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/ABCproger/card-validator/gen/card/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CardServiceName is the fully-qualified name of the CardService service.
	CardServiceName = "card.v1.CardService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CardServiceValidateProcedure is the fully-qualified name of the CardService's Validate RPC.
	CardServiceValidateProcedure = "/card.v1.CardService/Validate"
)

// CardServiceClient is a client for the card.v1.CardService service.
type CardServiceClient interface {
	Validate(context.Context, *connect.Request[v1.ValidateRequest]) (*connect.Response[v1.ValidateResponse], error)
}

// NewCardServiceClient constructs a client for the card.v1.CardService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCardServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CardServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	cardServiceMethods := v1.File_card_v1_card_proto.Services().ByName("CardService").Methods()
	return &cardServiceClient{
		validate: connect.NewClient[v1.ValidateRequest, v1.ValidateResponse](
			httpClient,
			baseURL+CardServiceValidateProcedure,
			connect.WithSchema(cardServiceMethods.ByName("Validate")),
			connect.WithClientOptions(opts...),
		),
	}
}

// cardServiceClient implements CardServiceClient.
type cardServiceClient struct {
	validate *connect.Client[v1.ValidateRequest, v1.ValidateResponse]
}

// Validate calls card.v1.CardService.Validate.
func (c *cardServiceClient) Validate(ctx context.Context, req *connect.Request[v1.ValidateRequest]) (*connect.Response[v1.ValidateResponse], error) {
	return c.validate.CallUnary(ctx, req)
}

// CardServiceHandler is an implementation of the card.v1.CardService service.
type CardServiceHandler interface {
	Validate(context.Context, *connect.Request[v1.ValidateRequest]) (*connect.Response[v1.ValidateResponse], error)
}

// NewCardServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCardServiceHandler(svc CardServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	cardServiceMethods := v1.File_card_v1_card_proto.Services().ByName("CardService").Methods()
	cardServiceValidateHandler := connect.NewUnaryHandler(
		CardServiceValidateProcedure,
		svc.Validate,
		connect.WithSchema(cardServiceMethods.ByName("Validate")),
		connect.WithHandlerOptions(opts...),
	)
	return "/card.v1.CardService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CardServiceValidateProcedure:
			cardServiceValidateHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCardServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedCardServiceHandler struct{}

func (UnimplementedCardServiceHandler) Validate(context.Context, *connect.Request[v1.ValidateRequest]) (*connect.Response[v1.ValidateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("card.v1.CardService.Validate is not implemented"))
}
